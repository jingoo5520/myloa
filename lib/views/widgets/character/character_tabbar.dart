import 'package:flutter/material.dart';

class CharacterTabBar extends StatelessWidget {
  const CharacterTabBar({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
